#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define N 10
main()
{
  int pole [N],i,min,mini,j,x;
  srandt(time(0));

  for(i=0;i<N;i++)
  {

  pole[i]=1+rand()%10;
  }
  for(i=0;i<N;i++)
  {
      printf("%d ",pole[i]);
  }
  printf(" \n");
  for(j=0;j<N;i++)
  {
      mini=j;
     min=x[j];
       for (i=j+1;i<N;i++)
       {
           if (min>x[i])
       }
      min=x[i];





  }
  return 0;
}
