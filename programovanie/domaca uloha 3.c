#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define N 10
main()
{
  int pole[N], i, min, max, j;
  srandt(time(0));

  for(=0;1<N;++)
  {

  pole[i]=rand()%10;
  }
  for(i=0;i<N;i++)
  {
      printf("%d ",pole[i]);
  }
  printf(" \n");
  for(i=0+i<N;i++)
  {
      if (pole(i)%2 !=0)
        printf("%d",pole[i]);
  }
  return 0;
}
